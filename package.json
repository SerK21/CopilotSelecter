{
  "name": "copilot-default-model",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Chrome/Edge extension to auto-select a default model in Copilot Premium",
  "scripts": {
    "test": "node --test tests/**/*.test.js",
    "icons": "python3 scripts/generate-icons.py"
  },
  "license": "MIT"
}
